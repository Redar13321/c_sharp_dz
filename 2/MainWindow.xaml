<Window x:Class="WpfApp4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Margin="10">

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="Доступно подключений:" Margin="0,0,5,0"/>
            <TextBlock x:Name="connectionStatus" Text="4/4" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock Text="Активных операций:" Margin="0,0,5,0"/>
            <TextBlock x:Name="activeOperations" Text="0" FontWeight="Bold" Margin="0,0,10,0"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Конфигурация БД (ReaderWriterLockSlim)" Margin="0,5">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Текущая конфигурация:" Width="150"/>
                    <TextBlock x:Name="currentConfig" Text="Не загружена" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Button x:Name="btnReadConfig" Content="Прочитать конфигурацию" 
                            Click="btnReadConfig_Click" Margin="0,0,5,0"/>
                    <Button x:Name="btnUpdateConfig" Content="Обновить конфигурацию" 
                            Click="btnUpdateConfig_Click"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5">
            <Button x:Name="btnStartConnections" Content="Запустить 8 подключений" 
                    Click="btnStartConnections_Click" Margin="0,0,5,0"/>
            <Button x:Name="btnCheckInstance" Content="Проверить экземпляр (Mutex)" 
                    Click="btnCheckInstance_Click"/>
        </StackPanel>

        <GroupBox Grid.Row="4" Header="Логи операций" Margin="0,5" Height="200">
            <ListBox x:Name="logListBox"/>
        </GroupBox>
    </StackPanel>
</Window>

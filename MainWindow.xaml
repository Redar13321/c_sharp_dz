<Window x:Class="WpfApp9.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:WpfApp9"
		mc:Ignorable="d"
		Background="#FFFDFAF1"
		Title="Список дел" Height="450" Width="800">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="50"/>
			<RowDefinition/>
			<RowDefinition Height="50"/>
		</Grid.RowDefinitions>

		<TextBlock Margin="10,0,10,0" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="26"> Список дел </TextBlock>

		<ListBox Name="listToDo" Grid.Row="1" Margin="10,10,10,10" Background="#FDF5DF" TextBlock.FontSize="14" VerticalContentAlignment="Top">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Path=Name}"/>
                        <TextBlock Text="{Binding Path=Date, StringFormat=dd.MM.yyyy}"/>
                        <TextBlock FontSize="10"  Text="{Binding Path=Description}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
		</ListBox>
        <CheckBox Name="toogleVisiblityToDo" Margin="10,0,10,0" VerticalAlignment="Bottom" Grid.Column="1" IsChecked="True" Click="toogleVisiblityToDo_Clicked">Добавление новых дел</CheckBox>
        <GroupBox Grid.Row="1" Grid.Column="1" Margin="10,10,10,10" Padding="5,5,5,5" Background="#FDF5DF" TextBlock.FontSize="14" Name="groupBoxToDo">
			<StackPanel>
				<TextBlock Margin="0,0,0,10">Название:</TextBlock>
				<TextBox Name="titleToDo"/>
				<TextBlock Margin="0,10,0,10">Выпонлить до:</TextBlock>
				<DatePicker SelectedDate="1.10.2024" Name="dateToDo"/>
				<TextBlock Margin="0,10,0,10">Описание:</TextBlock>
                <TextBox Text="Описания нет" Height="120" AcceptsTab="True" AcceptsReturn="True" Name="descriptionToDo" MaxLines="10"/>
			</StackPanel>
		</GroupBox>
		<Button Background="#5EBEC4" Grid.Row="2" MaxWidth="100" MaxHeight="30" Margin="10, 10, 10, 10" Name="buttonDelete" Click="buttonDelete_Click">
			Удалить дела
		</Button>
		<Button Content="Добавить дело" Background="#5EBEC4" Grid.Row="2" Grid.Column="1" MaxWidth="100" MaxHeight="30" Margin="10, 10, 10, 10" Name="buttonAdd" Click="buttonAdd_Click">
			<Button.ToolTip>
				<ToolTip>При добавлении дело появится в списке слева, и его придется выполнить</ToolTip>
			</Button.ToolTip>
		</Button>
	</Grid>
</Window>
